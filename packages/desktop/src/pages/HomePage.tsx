import { ChunkList } from '@english-chunks/shared';

export default function HomePage() {
  return (
    <main style={{ padding: '2rem', maxWidth: '1200px', margin: '0 auto' }}>
      <ChunkList />
    </main>
  );
}
